/*
import React from "react";

const RedLightGreenLightTimer = ({ status }) => {
  return (
    <div style={{ textAlign: "center", fontSize: "2rem", color: status === "Red Light" ? "red" : "green" }}>
      {status}
    </div>
  );
};

export default RedLightGreenLightTimer;
*/